var randomButtons = [];
var 

$('body').keypress(function() {
    $('h1').text('Level 1');
    for (i = Math.floor(Math.random() * 4) + 1; i < randomButtons.length; i++) {
        $('button')[i].push();
    }
})